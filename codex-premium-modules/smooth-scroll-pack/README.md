# smooth-scroll-pack

Lenis-based smooth scrolling helpers for premium marketing pages.

- Use `react/examples/LayeredParallaxScene.tsx` when a page needs layered atmospheric depth without a full immersive scene.
- Use `static/html/layered-parallax-scene.html` for the same pattern on template-driven or static sites.
